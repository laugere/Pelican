{% extends 'HomePage.html.twig' %} {% block body %}
<div id="content-container" class="scrollable">
    <div class="content-brand">
        <div class="row">
            <div class="col-9">
                <h4 class="content-title">
                    {% trans %}controllerName.notification{% endtrans %}
                </h4>
            </div>
        </div>
    </div>
    <div class="subcontainer scrollbar">
        {% if notifications is empty %}
        <h6 class="text-muted centered-message"><i class="fas fa-hotdog"></i> {% trans %}message.nocontent{% endtrans %}</h6>{% else %}{% for notification in notifications %} {% include ('vues/NotificationCard.html.twig') %} {% endfor %} {% endif %}
    </div>
</div>
{% endblock %} {% block javascripts %} {% endblock %}