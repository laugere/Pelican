<html lang="fr">
	<head>
		<title>
			Bonjour
			{{ userName }}
		</title>

		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body>
    <div class="container">
      <div id="mainFrame" class="card">
        <div
          class="card-body">
          <!-- NavBar -->
          {% include 'NavBar.twig' with {'pelican': "Pelican"} %}
        </div>
      </div>
    </div>
	</body>
</html>
